services:
  ameryd:
    build: .
    container_name: Ameryd
    restart: always
    ports:
      - "2021:2021"
    volumes:
      - /mnt/storage/Ameryd:/app/data
    environment:
      - PORT=2021
      - FLASK_ENV=production
